% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/htr_fill_missing.R
\name{htr_fill_missing}
\alias{htr_fill_missing}
\title{Replace missing values}
\usage{
htr_fill_missing(
  indir,
  outdir,
  method = "setmisstonn",
  neighbors = NULL,
  constant = NULL,
  overwrite = FALSE,
  ncores = NULL,
  hpc = NULL,
  file = NULL,
  cdo_flags = "-f nc4c -z zip_1"
)
}
\arguments{
\item{indir}{Character string. Directory containing merged NetCDF files to be
time-sliced. Files should be continuous time series created by \code{\link[=htr_merge_files]{htr_merge_files()}}.}

\item{outdir}{Character string. Directory where time-sliced files will be saved.}

\item{method}{Character string. Method used to calculate the missing value. Accepted methods are:
\itemize{
\item \code{"setmisstonn"}: Set missing values to the nearest neighbor's value
\item \code{"setmisstodis"}: Set missing values to the distance-weighted average of the neighbors.
The default number of neighbors is 4, but this can be changed by changing the \code{neighbors} parameter.
\item \code{"setmisstoc"}: Set missing values to a defined constant (requires \code{constant} parameter)
}}

\item{neighbors}{Numeric. Number of neighbors used to calculate missing values. Default is 4.}

\item{constant}{Numeric. Constant value used to replace all missing values.}

\item{overwrite}{Logical. If \code{FALSE} (default), skips files that already exist
in the output directory. If \code{TRUE}, regenerates files even if they exist.}

\item{ncores}{Integer or NULL. Number of CPU cores to use for parallel processing.
If \code{NULL} (default), uses all available cores minus 2. Ignored when \code{hpc = "array"}.}

\item{hpc}{Character string or NULL. Indicates High Performance Computing mode:
\itemize{
\item \code{NULL}: Standard processing mode (default)
\item \code{"array"}: HPC array job mode (requires \code{file} parameter)
\item \code{"parallel"}: HPC parallel mode
}}

\item{file}{Character string or NULL. Specific file to process when \code{hpc = "array"}.
Not used in other modes.}

\item{cdo_flags}{Character string. CDO output format flags for compression.
Default is \code{"-f nc4c -z zip_1"} for compressed NetCDF4 output.}
}
\value{
No return value. The function creates time-sliced files in the specified output
directory with the same base file names as the input.
}
\description{
This function replaces missing values in the climate model files using Climate Data
Operators (CDO), with values depending on the method chosen. Missing values can be
replaced by the nearest neighbor's values, the distance-weighted average of the neighbor's
values, or a constant.
}
\details{
The CDO command used depends on the chosen method
\itemize{
\item \strong{Nearest neighbor}: Uses \verb{cdo setmisstonn input output}
\item \strong{Distance-weighted average}: Uses \verb{cdo setmisstodis,neighbors input output}
\item \strong{Constant}: Uses \verb{cdo setmisstoc,constant input output}
}
}
\note{
\itemize{
\item Requires CDO (Climate Data Operators) to be installed and accessible from the system PATH
\item Uses parallel processing when \code{hpc} is not set to "array"
}
}
\examples{
\dontrun{
# Fill missing values using distance-weighted average of 7 neighbors
htr_fill_missing(
  indir = file.path(base_dir, "data", "proc", "integrated", "tos"),
  outdir = file.path(base_dir, "data", "proc", "filled", "tos"),
  method = "setmisstodis",
  neighbors = 7
)

# Fill missing values using the nearest neighbor's value
htr_fill_missing(
  indir = file.path(base_dir, "data", "proc", "integrated", "tos"),
  outdir = file.path(base_dir, "data", "proc", "filled", "tos")
)
}
}
\references{
CDO User Guide: https://code.mpimet.mpg.de/projects/cdo/embedded/cdo.pdf
CDO setmiss operator: https://code.mpimet.mpg.de/projects/cdo/embedded/index.html#x1-3610002.6.15
}
\author{
Tin Buenafe
}
