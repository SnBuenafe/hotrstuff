% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/htr_select_levels.R
\name{htr_select_levels}
\alias{htr_select_levels}
\title{Select vertical levels from depth-resolved climate data by index}
\usage{
htr_select_levels(
  indir,
  outdir,
  levidx,
  domain_name = "",
  overwrite = FALSE,
  ncores = NULL,
  hpc = NULL,
  file = NULL,
  cdo_flags = "-f nc4c -z zip_1"
)
}
\arguments{
\item{indir}{Character string. Directory containing merged NetCDF files to be
time-sliced. Files should be continuous time series created by \code{\link[=htr_merge_files]{htr_merge_files()}}.}

\item{outdir}{Character string. Directory where time-sliced files will be saved.}

\item{levidx}{Integer vector. The level index numbers (1-based) to select from
the input data. For example, \code{c(1, 2, 3)} selects the first three vertical
levels. Use \code{\link[=htr_show_levels]{htr_show_levels()}} to inspect available levels and their indices.}

\item{domain_name}{Character string. Optional suffix to add to output filenames
to identify the selected levels (e.g., "lev1-3", "surface"). Default is empty string.}

\item{overwrite}{Logical. If \code{FALSE} (default), skips files that already exist
in the output directory. If \code{TRUE}, regenerates files even if they exist.}

\item{ncores}{Integer or NULL. Number of CPU cores to use for parallel processing.
If \code{NULL} (default), uses all available cores minus 2. Ignored when \code{hpc = "array"}.}

\item{hpc}{Character string or NULL. Indicates High Performance Computing mode:
\itemize{
\item \code{NULL}: Standard processing mode (default)
\item \code{"array"}: HPC array job mode (requires \code{file} parameter)
\item \code{"parallel"}: HPC parallel mode
}}

\item{file}{Character string or NULL. Specific file to process when \code{hpc = "array"}.
Not used in other modes.}

\item{cdo_flags}{Character string. CDO output format flags for compression.
Default is \code{"-f nc4c -z zip_1"} for compressed NetCDF4 output.}
}
\value{
No return value. The function creates new NetCDF files containing only the
selected vertical levels in the specified output directory. If \code{domain_name}
is provided, it is added as a suffix to the original filename before the
file extension.
}
\description{
This function selects specific vertical levels from 3D ocean climate model data
using CDO (Climate Data Operators), based on level index numbers rather than
actual depth or pressure values. The selected levels are saved as a new NetCDF
file without any vertical integration.
}
\details{
The function processes depth-resolved ocean data (e.g., temperature, salinity)
and extracts a subset of vertical levels by their index position (1-based).
This is useful when you want to work with specific model levels regardless of
their actual depth or pressure coordinate values.

The CDO operation performed is:
\itemize{
\item \verb{cdo sellevidx,idx1,idx2,...} to extract the specified level indices
}

The function:
\enumerate{
\item Selects specific vertical levels by index using CDO sellevidx operator
\item Saves the result as a new NetCDF file (no vertical integration is performed)
\item Adds an optional domain name suffix to output filenames
}
}
\note{
\itemize{
\item Requires CDO (Climate Data Operators) to be installed and accessible from the system PATH
\item Input files must be 3D ocean data with vertical levels (depth or pressure coordinates)
\item Level indices are 1-based and correspond to the order of levels in the file
\item Use \code{\link[=htr_show_levels]{htr_show_levels()}} to inspect the available levels and their indices
\item Uses parallel processing when \code{hpc} is not set to "array"
}
}
\examples{
\dontrun{
# Select the first 3 vertical levels
htr_select_levels(
  indir = "path/to/3d/data",
  outdir = "path/to/output",
  levidx = c(1, 2, 3),
  domain_name = "top3levels"
)

# Select a single level by index
htr_select_levels(
  indir = "path/to/3d/data",
  outdir = "path/to/output",
  levidx = 1,
  domain_name = "surface"
)
}
}
\references{
CDO User Guide: https://code.mpimet.mpg.de/projects/cdo/embedded/cdo.pdf
CDO sellevidx operator: https://code.mpimet.mpg.de/projects/cdo/embedded/cdo.pdf#page=123
}
